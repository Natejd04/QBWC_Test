 <div id="page-wrapper" style="min-height: 629px;">
    <div class="row">
        <div class="col-lg-12">
			<h1 class="page-header">Customers</h1>

			<%=  will_paginate @cust1, renderer: BootstrapPagination::Rails %>			
			<div>
			<% @cust1.each_with_index do |c, index| %>
				<%= index + 1 %>.
				 <%= link_to c.name, customer_path(c.id) %><br/>
			<% end %>
			</div>
			



		</div>
	</div>
</div>