<% @invoices.each do |f| %>
<tr>
	<td><%= link_to f.c_name, customer_path(f.id) %><</td>
	<td><%= link_to f.c_invoicenumber, invoice_path(f.id) %></td>
	<td><%= f.c_date.strftime("%m/%d/%Y") %></td>
	<td><%= f.c_po %></td>
	<td><%= number_to_currency(f.c_subtotal, :unit=> "$") %></td>
</tr>
<% end %>	
<tr> 
	<td colspan="5"><b>TOTAL<div class="pull-right"><%= number_to_currency(@invoice_total, :unit => "$") %></div></b></td>
</tr>
